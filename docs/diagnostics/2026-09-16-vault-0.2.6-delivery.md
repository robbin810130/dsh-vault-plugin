# Vault 0.2.6 修复与交付记录

日期：2026-09-16。基于用户批准的 0.2.5 诊断，原始报告保持不变。
目标组合：Vault 0.2.6 + DSH 0.1.5-rc.1 + 本版本配套宿主补丁。

## 修复范围

- F01：完整锁拥有者记录原子发布；仅自动回收确认死亡的本机进程锁，永久回收标记避免并发 ABA。未知、空文件、外机或 PID 复用保守拒绝，并返回可诊断错误。
- F02：恢复密钥改为全局内存交付，不随行或设置组件卸载消失；明确保存确认和关闭页面警告。独立复核追加修复：成功响应的旧快照不能导致恢复密钥丢失。
- F03：快捷上锁不再复制原标题为组名；普通设置页及可访问标签遮蔽锁定组名称，不迁移或重命名真实数据。
- F04/F05：工作区上下文贯穿行、搜索、正文访问门禁和中央解锁插槽；首次快照未成功时保守遮蔽。修复折叠工作区子对话中央解锁按钮不可用。
- F06/F08：解锁、改密、恢复共用有界分组队列；同组串行，排队期间上锁会撤销尚未完成的授权工作。已进入同步提交的变更不能倒退撤销。
- F07：数字草稿与显式保存，保存中/失败提示；真实编辑起点冻结基线，逐叶子合并最新策略，同字段冲突提前拒绝并允许重新载入。不是跨浏览器原子 CAS。
- F10：持久 undo 记录保存原始主文件和备份，可在 SIGKILL 和恢复再次中断后重放恢复。
- 发布门禁：标签/双 manifest/说明一致性，插件测试、双类型检查及打包安装测试。

## 实际验证

| 检查 | 本轮结果 |
|---|---|
| 插件 Vitest | 32 文件 / 362 项通过 |
| 插件 host/client TypeScript | 两套均通过 |
| 安装/发布脚本 | 11 项通过 |
| 相关宿主测试 | 85 文件 / 1,414 项通过 |
| 宿主四个 browser bundles | 构建通过 |
| 精确上游补丁 | 24 文件正向/反向应用、源码逐字节相同 |
| 独立复核 | 两项并发问题修复并增加 12 项回归；状态锁/恢复复核未确认新增 P1 |
| 隔离 Chrome | 候选后端+候选四宿主 bundle；策略保存、工作区上锁、密钥交付、继承解锁、标题/正文 composer 恢复、刷新重新锁定通过 |
| 搜索与首次失败 | 搜索标题/摘要遮蔽；pending/首次失败快照不放出标题/composer |
| 真实 3080 | HTTP200，revision16，1组/1绑定；单 LaunchAgent PID67903；状态文件字节未变 |

搜索测试把一次性空对话在传输层标记为可搜索并注入合成摘要，不发模型请求；因此验证前台遮蔽，不冒充真实内容索引全链路测试。工作区创建、Vault 后端、策略保存及凭据调用没有模拟。会话快捷上锁及旧组名隐藏由组件回归覆盖，不冒充本轮新增浏览器全链路。

1440/390 宽度未见水平溢出；截图复核发现 390px 展开宿主侧栏时正文过窄，**不算移动端体验验收通过**。完整宿主类型检查仍有原有生成/依赖不匹配；不能拿构建与测试替代。

## 部署与回滚

备份：/Users/Robbin/.dsh/backups/vault-026-20260916-140201
manifest.json 记录原插件、八个宿主文件、配置、状态备份及哈希。已安装包文件与候选包一致，八个宿主文件哈希一致，state.json 与停服前字节一致。未更改密码、组名、绑定和既有策略。

部署第一轮遇到 LaunchAgent 卸载完成前立即 bootstrap 返回 EIO，已恢复原文件和服务后重试；第二轮等待服务注销完成再启动成功。保留首轮备份供排查，未遗留第二个监听进程。

回滚：停止 ai.deepseek.harness，等待 3080 无监听且 launchctl 中服务已注销；从上述备份 plugin 还原插件，从 manifest.hostFiles 恢复八个宿主文件，再 bootstrap 现有 plist。确认宿主仍为相同版本。**不应顺手覆盖当前 Vault 数据**；数据恢复需要另行判断。

候选插件包 SHA256：e93322ade982f72bf1c574532633de0c9a843b133e1365b09c21f49157550386
宿主补丁 SHA256：1159c60de0e2724f63a6cce6a04c2d574e4e4413a8a342aa5d035596f47e6215

## 后续迭代与边界

1. 下一批优先：F09 紧急 CLI 流式输入；版本探测 doctor、安全停服/安装/回滚工具，避免每次升级人工补宿主。
2. 再补：真实内容搜索、会话快捷上锁、跨页/跨标签/休眠唤醒、流式输出、子代理与右侧栏隐私矩阵；390px 宿主侧栏适配。
3. 文件事务边界：删除 undo 后目录 fsync 失败可能已提交，调用方应重读；state 与 audit 尚非崩溃原子事务。没有断电验收，没有跨进程服务器级 KDF 总量限制。
4. 仍是前台隐私锁，不是磁盘加密或宿主内容 API 鉴权；未知所有权锁需要停服人工恢复，不能按文件年龄删除。
5. 当前真实策略保持用户原值；本轮不擅自增强最小密码长度或冷却。

GitHub 发布与下载校验结果以最终交付消息和 Release 工作流为准；本记录不提前声称远端发布完成。
