# DSH Vault 0.2.10

- Keep Workspace protection indicators in the Workspace row; set and bind quick locks to the Workspace itself.
- Conceal protected Workspace and Session titles in the sidebar, Conversation title, and browser title.
- Add a fail-closed unlock guard over protected Conversation content.
- Keep the DSH 0.1.7-rc.1 host patch and generated client bundles pinned to exact upstream source and hashes.
- Preserve existing Vault password groups, bindings, and access policy; no Vault state migration or reset.
