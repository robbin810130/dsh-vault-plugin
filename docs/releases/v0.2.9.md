# DSH Vault 0.2.9

修复 DSH `0.1.7-rc.1` 下无法从 Workspace 行加锁的问题。兼容补丁为真实 Workspace 行声明 accessory/action 槽位，Vault 通过行内操作创建工作区保护组，并在工作区受保护时显示锁定状态；未归属 Workspace 的会话分组不会出现工作区操作。

本版本继续使用 Vault 的现有密码组、绑定与访问控制流程，不迁移或重置 Vault 状态。
