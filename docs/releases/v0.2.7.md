# dsh-vault-plugin v0.2.7 — unscoped package identity

- Rename actual package to dsh-vault-plugin, including manifests, bundle identity, installation instructions and regression tests.
- Preserve node IDs, configuration and state/backup paths. GitHub ownership/URLs remain unchanged; no npm publication.
- Stop DSH and back up the complete profile and state before migrating. Remove the old scoped package before adding the new one; never load both. Restore unrelated plugin data stored inside node_modules before restarting. See README.
- Passwords, policy and bindings are preserved. One-line installers do not remove old packages automatically. Runtime business logic and the DSH 0.1.5-rc.1 host patch are unchanged from 0.2.6. Foreground privacy only, not content encryption or host content API authorization.
